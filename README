# Save state for Fragments

Theres a variable named *saved* in `FirstFragment`with initially value of `false`.
While we change the value of it to `true` if you rotate the device variable *saved*
loses its state and the toast we expect to see it doesn't appears.
 
